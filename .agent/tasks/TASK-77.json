{
  "id": "TASK-77",
  "title": "Página 'Minha Assinatura' com plano ativo e botão de gerenciar UI",
  "category": "ui-ux",
  "description": "Página `/dashboard/assinatura` onde o aluno visualiza detalhes da sua assinatura ativa e acessa o Stripe Customer Portal.",
  "acceptanceCriteria": [
    "Exibe: nome do plano, ciclo de cobrança (mensal/anual), status (badge), próxima renovação, valor",
    "Botão 'Gerenciar Assinatura' chama POST /api/subscriptions/portal e redireciona para URL retornada",
    "Status past_due: exibir alerta 'Pagamento pendente — sua assinatura está temporariamente suspensa'",
    "Sem assinatura: exibir CTA 'Assinar agora' com link para /planos"
  ],
  "steps": [
    {
      "step": 1,
      "description": "Criar página de assinatura",
      "details": "Criar `src/app/(app)/dashboard/assinatura/page.tsx`. Buscar dados de `GET /api/users/me` + `GET /api/subscriptions/current` (ou incluir assinatura no response de /api/users/me). Renderizar informações do plano. Formatar `currentPeriodEnd` como data legível.",
      "pass": false
    },
    {
      "step": 2,
      "description": "Botão de gerenciar assinatura",
      "details": "Client Component com botão 'Gerenciar Assinatura'. Ao clicar: POST /api/subscriptions/portal → redirecionar para `portalUrl`. Mostrar loading durante a requisição.",
      "pass": false
    }
  ],
  "dependencies": ["TASK-76", "TASK-17"],
  "estimatedComplexity": "low",
  "technicalNotes": []
}
