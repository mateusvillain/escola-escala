{
  "id": "TASK-166",
  "title": "Autorização por papel dentro da organização (owner vs member)",
  "category": "security",
  "description": "Garantir de forma centralizada que apenas o `owner` de uma organização pode convidar/remover membros, iniciar ou alterar a assinatura B2B e acessar o Customer Portal da organização; membros (`role: 'member'`) têm acesso somente de leitura aos dados da própria organização.",
  "acceptanceCriteria": [
    "`POST /api/organizations/me/invites`, `DELETE /api/organizations/me/members/[userId]`, `POST /api/organizations/me/checkout` e `POST /api/organizations/me/portal` retornam 403 para usuários com `role: 'member'`",
    "`GET /api/organizations/me` funciona para qualquer membro, mas a UI (TASK-164) omite os controles de gestão para `member`",
    "Usuário sem organização recebe 404 (não 403) ao acessar `/api/organizations/me`, para não revelar a existência de organizações de outros usuários",
    "Teste automatizado cobre um `member` tentando convidar/remover/checkout e recebendo 403 em todos os casos"
  ],
  "steps": [
    {
      "step": 1,
      "description": "Criar helper requireOrgOwner",
      "details": "Criar `requireOrgOwner(userId: string)` em `src/lib/access.ts` (ou novo `src/lib/organization.ts`), que busca o `OrganizationMember` do usuário e retorna a organização se `role === 'owner'`, ou lança/retorna um erro 403 padronizado caso contrário.",
      "pass": true
    },
    {
      "step": 2,
      "description": "Aplicar o helper nos endpoints de gestão",
      "details": "Usar `requireOrgOwner` em `src/app/api/organizations/me/invites/route.ts` (TASK-158) e `src/app/api/organizations/me/members/[userId]/route.ts` (TASK-161), seguindo o mesmo padrão `instanceof NextResponse` usado por `requireRole`. `src/app/api/organizations/me/checkout/route.ts` (TASK-162) e `src/app/api/organizations/me/portal/route.ts` (TASK-164) ainda não existem neste grupo (Grupos 6/7 da Fase 4) — quando forem criados, devem usar este mesmo helper desde o início, em vez de duplicar a checagem de papel.",
      "pass": true
    },
    {
      "step": 3,
      "description": "Escrever testes de autorização",
      "details": "Em `src/lib/__tests__/organization.test.ts`, testes (Vitest) cobrindo `requireOrgOwner` para owner, member e usuário sem organização — como os endpoints de invites/remoção chamam esse helper antes de qualquer ação, cobre os dois endpoints existentes (e os futuros de checkout/portal igualmente). 404 em `GET /api/organizations/me` sem organização já estava implementado na TASK-157 e foi validado manualmente.",
      "pass": true
    }
  ],
  "dependencies": [
    "TASK-152",
    "TASK-158",
    "TASK-161",
    "TASK-162"
  ],
  "estimatedComplexity": "low"
}