{
  "id": "TASK-167",
  "title": "Modelos CourseTrack e CourseTrackItem",
  "category": "data-model",
  "description": "Criar os models que permitem agrupar cursos relacionados em uma trilha (sequência com progressão), base para trilhas e bundles de cursos.",
  "acceptanceCriteria": [
    "Model `CourseTrack` com `id`, `title`, `slug` (único), `description`, `thumbnailUrl String?`, `status CourseStatus` (reaproveita o enum já existente), `createdAt`, `updatedAt`",
    "Model `CourseTrackItem` com `id`, `trackId`, `courseId`, `order Int`, e `@@unique([trackId, courseId])` para impedir o mesmo curso duplicado na trilha",
    "Migration aplicada sem alterar `Course` nem nenhuma tabela existente"
  ],
  "steps": [
    {
      "step": 1,
      "description": "Adicionar os models ao schema",
      "details": "Em `prisma/schema.prisma`:\n```prisma\nmodel CourseTrack {\n  id           String       @id @default(uuid())\n  title        String\n  slug         String       @unique\n  description  String\n  thumbnailUrl String?\n  status       CourseStatus @default(draft)\n  createdAt    DateTime     @default(now())\n  updatedAt    DateTime     @updatedAt\n\n  items CourseTrackItem[]\n}\n\nmodel CourseTrackItem {\n  id       String      @id @default(uuid())\n  trackId  String\n  track    CourseTrack @relation(fields: [trackId], references: [id], onDelete: Cascade)\n  courseId String\n  course   Course      @relation(fields: [courseId], references: [id])\n  order    Int\n\n  @@unique([trackId, courseId])\n}\n```",
      "pass": true
    },
    {
      "step": 2,
      "description": "Adicionar relação inversa em Course",
      "details": "Adicionar `trackItems CourseTrackItem[]` ao model `Course`.",
      "pass": true
    },
    {
      "step": 3,
      "description": "Rodar a migration",
      "details": "Rodar `npx prisma migrate dev --name add_course_track` após também adicionar os campos de bundle da TASK-168 (mesma migration).",
      "pass": true
    }
  ],
  "dependencies": ["TASK-146"],
  "estimatedComplexity": "low"
}
