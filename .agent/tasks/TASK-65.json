{
  "id": "TASK-65",
  "title": "E-mail de boas-vindas disparado após primeira assinatura ativa",
  "category": "functional",
  "description": "Enviar e-mail de boas-vindas ao aluno quando a primeira assinatura é ativada (webhook checkout.session.completed).",
  "acceptanceCriteria": [
    "E-mail enviado após webhook `checkout.session.completed` ativar a assinatura",
    "Conteúdo: boas-vindas com nome do aluno, plano ativado, link para o dashboard",
    "Enviado apenas na primeira ativação (não em renovações)"
  ],
  "steps": [
    {
      "step": 1,
      "description": "Integrar envio no webhook",
      "details": "Em TASK-71 (handler do webhook checkout.session.completed): após criar UserSubscription no banco, verificar se é o primeiro plano do usuário (`where: { userId, COUNT(*) = 1 }`). Se sim: chamar `sendWelcomeEmail(user.email, user.name)`. Usar try/catch para não bloquear o processamento do webhook.",
      "pass": false
    }
  ],
  "dependencies": ["TASK-64", "TASK-71"],
  "estimatedComplexity": "low",
  "technicalNotes": []
}
