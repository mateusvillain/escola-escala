{
  "id": "TASK-197",
  "title": "Componente EmbeddedCheckoutForm reutilizável",
  "category": "ui-ux",
  "description": "Para renderizar o checkout embutido (TASK-196) é necessário instalar `@stripe/stripe-js` e `@stripe/react-stripe-js` e criar um componente client reutilizável que recebe um `clientSecret` e monta o iframe do Stripe na página — usado inicialmente no fluxo de cadastro (TASK-199), mas desenhado para ser reaproveitável em outros pontos do produto no futuro (ex.: upgrade de plano autenticado).",
  "acceptanceCriteria": [
    "`@stripe/stripe-js` e `@stripe/react-stripe-js` adicionados a `package.json`",
    "`loadStripe` chamado uma única vez (módulo singleton, ex.: `src/lib/stripe-client.ts`) com `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`",
    "Componente `EmbeddedCheckoutForm` (`src/components/checkout/EmbeddedCheckoutForm.tsx`) recebe `clientSecret: string` e `onComplete?: () => void` via props, envolve `<EmbeddedCheckout>` com `<EmbeddedCheckoutProvider>`, e exibe um estado de loading enquanto a sessão do Stripe carrega",
    "Erros de carregamento da sessão (ex.: `clientSecret` inválido/expirado) são capturados e exibem uma mensagem amigável em vez de tela em branco"
  ],
  "steps": [
    {
      "step": 1,
      "description": "Instalar dependências",
      "details": "`npm install @stripe/stripe-js @stripe/react-stripe-js`.",
      "pass": true
    },
    {
      "step": 2,
      "description": "Criar o singleton do Stripe.js no client",
      "details": "Em `src/lib/stripe-client.ts`: `import { loadStripe } from '@stripe/stripe-js'; export const stripePromise = loadStripe(process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY!)`. Não chamar `loadStripe` mais de uma vez na árvore de componentes (evita recriar a conexão a cada render).",
      "pass": true
    },
    {
      "step": 3,
      "description": "Criar o componente EmbeddedCheckoutForm",
      "details": "`'use client'` component em `src/components/checkout/EmbeddedCheckoutForm.tsx`, usando `EmbeddedCheckoutProvider` (`stripe={stripePromise}`, `options={{ clientSecret, onComplete }}`) e `<EmbeddedCheckout />` do `@stripe/react-stripe-js`. Renderizar um spinner/skeleton enquanto o provider inicializa.",
      "pass": true
    },
    {
      "step": 4,
      "description": "Validar isoladamente",
      "details": "Criar uma página/rota temporária (ou usar o Storybook se existir) passando um `clientSecret` real obtido manualmente da TASK-196 e confirmar que o formulário de pagamento do Stripe renderiza dentro da página, sem redirecionar.",
      "pass": true
    }
  ],
  "dependencies": [
    "TASK-196"
  ],
  "estimatedComplexity": "low",
  "technicalNotes": [
    "`EmbeddedCheckoutProvider` precisa ser remontado (`key` diferente) se o `clientSecret` mudar para uma sessão totalmente nova — não tente atualizar o `clientSecret` de uma sessão já montada."
  ]
}
