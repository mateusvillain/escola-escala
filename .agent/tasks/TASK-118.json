{
  "id": "TASK-118",
  "title": "POST /api/courses/[slug]/reviews",
  "category": "api-endpoint",
  "description": "Endpoint para o aluno enviar (ou atualizar) sua avaliação de um curso que já concluiu, e para listar avaliações públicas.",
  "acceptanceCriteria": [
    "`POST` requer autenticação e `CourseEnrollment.completedAt` preenchido para o curso — sem isso retorna 403",
    "`rating` validado via Zod (inteiro 1 a 5); `comment` opcional, máx. 1000 caracteres",
    "Upsert por `userId`+`courseId` — reenviar atualiza a avaliação existente em vez de duplicar",
    "`GET` retorna lista paginada de avaliações públicas (rating, comment, createdAt e nome do autor)"
  ],
  "steps": [
    {
      "step": 1,
      "description": "Criar o route handler",
      "details": "Criar `src/app/api/courses/[slug]/reviews/route.ts` com handlers `GET` e `POST`.",
      "pass": false
    },
    {
      "step": 2,
      "description": "Validar conclusão do curso",
      "details": "Validar `CourseEnrollment.completedAt` antes de aceitar o `POST`.",
      "pass": false
    },
    {
      "step": 3,
      "description": "Implementar upsert",
      "details": "Implementar upsert por `userId_courseId` no `POST`.",
      "pass": false
    },
    {
      "step": 4,
      "description": "Implementar listagem paginada",
      "details": "Implementar `GET` paginado retornando `rating`, `comment`, `createdAt` e nome do autor.",
      "pass": false
    }
  ],
  "dependencies": [
    "TASK-117",
    "TASK-55"
  ],
  "estimatedComplexity": "medium"
}
